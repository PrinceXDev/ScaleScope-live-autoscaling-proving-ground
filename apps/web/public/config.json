{
  "apiUrl": "https://gateway-2e58-3000.prg1.zerops.app",
  "oracleUrl": "https://oracle-2e58-3000.prg1.zerops.app",
  "_comment": "Set apiUrl to your deployed gateway service's public URL and oracleUrl to the oracle service's public URL. Oracle serves /params and /accuracy directly to the browser with an open CORS policy (read-only, no credentials, by design - see apps/oracle/src/index.js). Leave either empty to fall back to same-origin. Editing this file needs only a static redeploy, not a rebuild."
}
